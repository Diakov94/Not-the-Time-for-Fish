zone: tools/headless
size: S
# A two-client game inside the gates

One 10 s two-client headless run inside `npm run gates`, its wall time printed.

## DoD
- The gates fail when the headless game fails.

## Acceptance
- `npm run gates` takes at most 60 s on a free machine; the number is in the report.

## Test
- None: the gates are the check.
