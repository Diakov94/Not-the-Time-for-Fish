zone: src/app
size: M
files: src/app/screens/lobby.ts, index.html
# The lobby with six characters per side, in the game's style

GAME.md, Menus: the lobby with the roster and the next round's dogs (card 162), the map pick and the voice-channel reminder. Card 47's lobby names three characters per side from its own list; card 100 made the roster the owner of twelve.

## DoD
- Six characters per side from the roster (name and signature detail), the picker as one row per side; the host's controls; the current map's name from the maps the app knows (card 101); the pick itself is card 145.
- The same style as the menu (card 118): the palette, the type, `var(--scale, 1)`.

## Acceptance
- 6 names per side shown (before 3); at 1280×720 with 8 players no overflow, margin named; the worst button ≥ 44×44 px.

## Test
- None: layout and text.
