zone: src/app
size: XS
# The results screen names the rule that decided a match

GAME.md, Round Structure: the team that secured more fish wins the match; on equal counts, the team whose last fish was secured sooner in its round wins; 0–0 is a draw. The results screen names no rule that decided a match (batch A1's report): it prints the whole rule beside every outcome, so a 2–2 match reads the same as a 3–2 one.

## DoD
- The round table owns which rule decided the match, born where the match's outcome is; the screen only shows it, in Ukrainian.

## Acceptance
- A 2–2 match's results screen names the faster last fish; a 0–0 match says draw. Before: it names no rule.

## Test
- None: the screen's text for a 2–2, a 3–2 and a 0–0 match in the report.
