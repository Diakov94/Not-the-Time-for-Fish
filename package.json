{
  "name": "ne-chas-dlia-rybky",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "play": "vite build && vite preview",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "zones": "! grep -rslE \"(from|import)[ (]*['\\\"]three|document\\.|window\\.|navigator\\.\" src/sim src/net src/relay src/content | grep . && ! grep -rslE \"(src/|\\.\\./|@)(render|app|hud|audio|art|settings|input|meta)/\" src/sim src/net src/relay src/content | grep . && ! grep -rslE \"(from|import)[ (]*['\\\"]((\\.\\./)+(sim|net|relay|tools)/|@dimforge)\" src/content | grep . && ! grep -rslE \"(src/|\\.\\./|@)(sim|net|app|hud|audio)/\" src/art | grep . && ! grep -rslE \"(src/|\\.\\./|@)(sim|net|relay|content|render|app|hud|audio|art|input|meta)/\" src/settings | grep . && ! grep -rslE \"(src/|\\.\\./|@)(render|app|hud)/\" src/meta | grep .",
    "test": "vitest run --passWithNoTests",
    "gates": "npm run typecheck && npm run lint && npm run zones && npm run build && npm run test && npm run headless -- --clients 2 --seconds 10",
    "relay": "node src/relay/serve.ts",
    "headless": "node tools/headless/main.ts"
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "^0.20.0",
    "three": "^0.186.0",
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "@types/three": "^0.186.0",
    "@types/ws": "^8.18.1",
    "eslint": "^10.11.0",
    "typescript": "~6.0.3",
    "typescript-eslint": "^8.70.1",
    "vite": "^8.3.0",
    "vitest": "^5.0.1"
  }
}
